apiVersion: v1
kind: Service

metadata:
  name: nginx-service
  namespace: fota

spec:
  #找所有 label 中 app=nginx 的 Pod
  selector:
    app: nginx

  ports:
    - port: 80
      targetPort: 80

  type: ClusterIP